#include<iostream>
#include<deque>
#include<algorithm>
using namespace std;
int main(){
	vector<int> v;	//定义一个存储int型元素的vector
	deque<double> d;
	//......
	sort(v.begin(), v.end());
	sort(d.begin(), d.end());
	//......
}
