	来自“怎么配平资源”
	有些C和C++开发者故意在解除了某个指针引用的内存的分配后，将该指针设为NULL。这为什么是一个好主意

	解答：在大多数C和C++的实现中，没有检查指针实际上是否指向有效内存的途径。一种常见的错误是：解除了某个内存块的分配，随后又在程序中引用该内存。此时，该指针所指向的内存很可能已经重新分配用于其他目的。通过把指针设置为NULL，程序员希望能预防这些有害的引用——在大多数情况下，解除NULL指针的引用将生成运行时错误
