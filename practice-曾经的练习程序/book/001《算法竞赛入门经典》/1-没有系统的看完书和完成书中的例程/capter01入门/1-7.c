#include<stdio.h>
/*1-6.c还有一个没有考虑到的细节，如果各位书是0,反转后应该输出吗？例如输入是
 *250，输出是052还是52,所以需要考虑到
 */
int main(){
	int n,m;
	scanf("%d",&n);
	m = (n%10)*100+(n/10%10)*10+(n/100);
	printf("%03d\n",m);
	return 0;
}
