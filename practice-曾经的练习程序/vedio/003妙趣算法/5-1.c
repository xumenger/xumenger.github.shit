#include<stdio.h>
#include<math.h>

int main()
{
	double sum = 0.0;
	int i;
//	for(i=1; i<=10; i++)
//		sum += pow(2, i-1);
	sum = pow(2, 10);
	printf("The number of the grain is %f \n", sum);

	return 0;
}
